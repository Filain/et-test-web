const baseURL = 'http://localhost:3003';

const event='/event';
const users='/users';

const urls={
    events: {
        base: event
    },
    users: {
        base: users
    }
}
export {
    baseURL,
    urls
}